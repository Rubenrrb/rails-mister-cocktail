
<h2><% @cocktail.doses.each do |dose| %></h2>
  <h2><%= dose.description %></h2>
  <h2><%= dose.ingredient.name %></h2>

<%= link_to dose_path(dose), method: :delete, data: { confirm: "delete it?" } do %>
        <i class="far fa-trash-alt"></i>
        <% end %>
<% end %>

<%= link_to "add doses", new_cocktail_dose_path(@cocktail)  %>




<!-- 
<%= link_to cocktail_path(@cocktail), method: :delete, data: { confirm: "destroy?" } do %>
        <i class="far fa-trash-alt"></i>
        <% end %>